# Security Model

> Documento em construção.

## Escopo

Modelo de segurança da aplicação, incluindo autenticação (Supabase Auth), Row Level Security (RLS) e políticas de acesso do `apps/runner`.

## Status

Aguardando definição arquitetural detalhada pelo Arquiteto.
