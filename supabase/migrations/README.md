# Migrations

Diretório destinado às migrations SQL do banco de dados (PostgreSQL via Supabase).

Nenhuma migration foi criada nesta etapa. O schema definitivo depende de definição arquitetural prévia (ver `docs/database/schema.md`).
